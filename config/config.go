package config

// SortSpeed is used in both main.go and sorting.go where the user adjusts the speed in main.go
// and sorting.go reads this value so there can be live changes in speed
var SortSpeed = 500

// Stop is used in both main.go and sorting.go
var Stop = false

var RunningTotal = 0

var SortType = 0
